import React from "react";
import { ChatContainer } from "../modules/chat/ChatContainer";
const ChatPage = (props) => {
  return (
    <>
      {console.log(props)}
      <ChatContainer />
    </>
  );
};
export { ChatPage };
